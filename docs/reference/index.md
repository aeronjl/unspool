# Python API

The root package exposes Unspool's public modelling, validation, recovery, diagnostics, and
interoperability surface. Optional data-source and optimization dependencies are required
only when their adapters are used.

::: unspool
